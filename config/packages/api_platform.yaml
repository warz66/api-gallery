api_platform:
    #defaults:
        #pagination_enabled: false
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/ld+json']
    swagger:
        versions: [3]
